// Metadata - Cleared for fresh start
export const Metadata = ({ pageTitle = "Page", pageDescription = "Test page" }) => {
  return (
    <div className="hidden">
      {/* Metadata cleared for fresh start */}
    </div>
  );
};